#include "main.h"

/**
 * _isdigit - function that checks for digit
 * @c: the intger it takes
 * Return: 0 if false and 1 if true
 */
int _isdigit(int c)
{
	if (c >= '0' && c <= '9')
{
	return (1);
}
else
{
	return (0);
}
}
