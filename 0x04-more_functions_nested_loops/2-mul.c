#include "main.h"

/**
 * mul - function that multiplies
 * @a: the integer it takes
 * @b: the integer it takes
 * Return: 0 if false, 1 if true
 */
int mul(int a, int b)
{
	int multiply;

	multiply = a * b;

	return (multiply);
}
