#include <stdio.h>

/**
 * main - A function
 * @argv: The argument vector
 * @argc: The argument count
 * Return: The result
 */
int main(int argc, char* argv[])
{
	if (argc > 0)
{
	printf("%s\n", argv[0]);
}
	return (0);
}
