#include "main.h"

/**
 * main - Entry point
 *
 * Description: Determines the state of a value
 *
 * Return: Always 0 (success)
 */
int main(void)
{
	int i;

	i = 0;
	positive_or_negative(i);

	return (0);
}
